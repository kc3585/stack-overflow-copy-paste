import test from 'ava'
import {printWords} from '../src'

test('print correct words', t => {
  const original = "word"
  const expected = word
  const actual = printWords(original)
  t.deepEqual(acutal, expected)
})
